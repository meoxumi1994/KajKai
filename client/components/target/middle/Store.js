import React from 'react';

import Top from '~/containers/target/middle/Top'

const Store = (props) => {
    return(
        <div>
            <Top {...props}/>
        </div>
    )
}

export default Store
