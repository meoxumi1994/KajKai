import React from 'react'

const Info = ({ INFO, }) => {
    return(
        <div className="panel panel-default">
            <div className="panel-heading">{INFO}</div>
        </div>
    )
}

export default Info
